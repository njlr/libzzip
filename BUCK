cxx_library(
  name = 'libzzip',
  header_namespace = 'zzip',
  exported_headers = subdir_glob([
    ('zzip', '*.h'),
  ]),
  srcs = glob([
    'zzip/*.c',
  ]),
  visibility = [
    'PUBLIC',
  ],
)
